# CLAUDE.md

## クラウドルーティンとして動いている場合（先に読む）

このリポジトリは、毎朝動く**クラウドルーティン2本**（ポケセンオンライン抽選チェック /
沖縄ポケカ抽選チェック）の `sources` に指定されている。**あなたがそのルーティンとして
起動されているなら、以下の「セッションの引き継ぎとコミット」は適用されない。**

ルーティンは次をしないこと:

- `HANDOFF.md` を書き換える
- `git commit` / `git push` する

理由: 引き継ぎは「人間が作業を再開するための設計メモ」であって実行ログではない。
毎朝の実行記録で上書きすると、設計の経緯が消える。実際 2026-08-27 に一度これが起きた
（commit `fe0b46f`）。実行結果は最終出力の実行ログとして報告すれば足りる。

ルーティンがこのリポジトリから使うのは `scripts/notify-line.mjs`（LINE への Flex 送信）だけ。

## セッションの引き継ぎとコミット（人間との作業セッションで必須）

### 始めるとき: `HANDOFF.md` を読む

リポジトリ直下の `HANDOFF.md` に前回までの状況が書いてある。**作業を始める前に必ず読む。**
無ければ、このセッションの終わりに新規作成する。

### 終わるとき 1: `HANDOFF.md` を書き直す

**このファイルだけ読めば作業を再開できる**状態に上書きする。書くこと:

| 項目 | 内容 |
|---|---|
| 最終更新 | 絶対日付で書く（「今日」「先週」とは書かない） |
| いま何をしているのか | 目的と、いま立っている場所 |
| 今回やったこと | 変更したファイル・実行したコマンド・その結果 |
| 検証済みの事実 | テスト結果・実行ログ・スクショなど、**実際に画面に出した出力**だけを「確認済み」と書く |
| 未検証のもの | 動かしていない・確かめていないものは「未検証」と明記する |
| 次にやること | 手が動かせる粒度で。必要なコマンドはそのまま貼る |
| 触ってはいけないところ | 制約・既知の失敗・保留中の判断 |

- **古くなった記述は消して書き直す。** 継ぎ足して長くしない。履歴は git log に残る
- **未検証の推測を「確認済み」として書かない。** 引き継ぎに残った誤りは、次のセッションが前提として積み上げてしまう

### 終わるとき 2: コミットして push、既定ブランチへマージする

**確認は取らずに実行する。**

```bash
git add -A
git commit -m "<何を変えたか>"
git push
```

作業ブランチにいる場合は、既定ブランチ（通常は `main`）へマージするところまで行う:

```bash
git checkout main && git pull && git merge --no-ff <作業ブランチ> && git push origin main
```

PR は不要（本人のみが使う private リポジトリのため）。明示的に依頼されたときだけ作る。

途中で終わるとき・失敗したときも、**そこまでの状態をコミットして残す。**
何が未完了かは `HANDOFF.md` に書く。
